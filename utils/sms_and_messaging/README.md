# SMS & Messaging Module

The module's core function is `send_SMS(recipient: str, message: str)`

the `recipient` parameter should be a phone number which adheres
to the **E.164** format.

_ex: +639XXXXXXXXX_
