[project]
name = "SMS-and-Messaging-Module"
version = "1.0.0"
description = "This is module allows for sending SMS via HTTPSMS API"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "python-dotenv>=1.1.1",
    "requests>=2.32.5",
]
